[package]
name = "meshly-core-client-gui"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
description = "meshly-core client GUI frontend (placeholder for v1)"

[lib]
path = "src/lib.rs"

# v1: stub library. Future: GUI frontend (egui or iced).
[dependencies]
meshly-core-common.workspace = true